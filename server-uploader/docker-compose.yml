version: '3.9'
services:
  mongo:
    image: mongo:7
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: stuzync
    volumes:
      - mongo_data:/data/db
volumes:
  mongo_data: {}

